name: Build MAME (libretro) Linux ARM64 (TSP)

on:
  workflow_dispatch:
    inputs:
      commit_sha:
        description: 'Full commit SHA to clone up to (leave blank for latest/current repo)'
        required: false
        type: string
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Maximize Runner Disk Space & Create Swap
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/boost
          sudo docker image prune --all --force
          # Allocate swap space to prevent the LTO linker stage from running out of memory
          sudo fallocate -l 8G /swapfile
          sudo chmod 600 /swapfile
          sudo mkswap /swapfile
          sudo swapon /swapfile

      - name: Checkout MAME
        run: |
          if [ -n "${{ inputs.commit_sha }}" ]; then
              echo "Cloning repository and checking out specific commit: ${{ inputs.commit_sha }}"
              git init mame
              cd mame
              git remote add origin https://github.com/libretro/mame.git
              git fetch --depth 1 origin ${{ inputs.commit_sha }}
              git checkout FETCH_HEAD
            else
              echo "Cloning latest repository (shallow clone)"
              git clone --depth 1 https://github.com/libretro/mame.git mame
              cd mame
            fi
          git submodule update --init --recursive --depth 1

      - name: CCache Initialization
        uses: actions/cache@v4
        with:
          path: .ccache
          key: mame-ccache-${{ github.run_id }}
          restore-keys: |
            mame-ccache-

      - name: Build in Ubuntu 21.04 Container
        run: |
          cat << 'EOF' > build_mame.sh
          #!/bin/bash
          set -e
          
          echo '=== Resurrecting Ubuntu 21.04 Repositories ==='
          sed -i -re 's/([a-z]{2}\.)?archive.ubuntu.com|security.ubuntu.com/old-releases.ubuntu.com/g' /etc/apt/sources.list
          
          echo '=== Setting up Build Environment ==='
          apt-get update
          apt-get install -y software-properties-common wget python3 pkg-config \
                  build-essential git unzip ccache coreutils make xz-utils
          
          echo '=== Installing Native & Cross GCC-11 ==='
          apt-get install -y gcc-11 g++-11 gcc-11-aarch64-linux-gnu \
                  g++-11-aarch64-linux-gnu binutils-aarch64-linux-gnu
          
          echo '=== Deploying Custom SDL2 ==='
          wget -q https://raw.githubusercontent.com/Ultrahead/EmuBuildsTSP/main/blobs/SDL2-2.26.1_TSP.zip
          mkdir -p /sdl2
          unzip -q SDL2-2.26.1_TSP.zip -d /sdl2
          chmod -R +x /sdl2/bin
          
          export PATH="/sdl2/bin:$PATH"
          export CCACHE_DIR=/root/.ccache
          ccache -M 5G
          
          echo '=== NUCLEAR PURGE: Eradicating x86 flags ==='
          cd /build/mame
          grep -rIl '\-m64' . | xargs sed -i 's/-m64//g' || true
          grep -rIl '\-m32' . | xargs sed -i 's/-m32//g' || true
          
          echo '=== Building GENie (Natively) ==='
          cd /build/mame/3rdparty/genie/build/gmake.linux
          make clean
          make -j$(nproc) CC=gcc-11 CXX=g++-11
          
          echo '=== Compiling FULL MAME with Cortex-A53 Tuning & LTO ==='
          cd /build/mame
          make -f Makefile.libretro \
                -j$(nproc) \
                platform=unix-armv8 \
                PLATFORM=arm64 \
                TARGETOS=linux \
                PTR64=1 \
                USE_LUA=1 \
                NOWERROR=1 \
                CROSS_BUILD=1 \
                HOSTCC='ccache gcc-11' \
                HOSTCXX='ccache g++-11' \
                CC='ccache aarch64-linux-gnu-gcc-11' \
                CXX='ccache aarch64-linux-gnu-g++-11' \
                AR='aarch64-linux-gnu-gcc-ar-11' \
                PYTHON_EXECUTABLE=python3 \
                EXTRACFLAGS='-I/sdl2/include/SDL2 -O3 -mcpu=cortex-a53 -mtune=cortex-a53 -flto --param ggc-min-expand=10' \
                EXTRACXXFLAGS='-I/sdl2/include/SDL2 -O3 -mcpu=cortex-a53 -mtune=cortex-a53 -flto --param ggc-min-expand=10' \
                LDFLAGS='-L/sdl2/lib -Wl,--hash-style=both -O3 -mcpu=cortex-a53 -mtune=cortex-a53 -flto'
                
          aarch64-linux-gnu-strip /build/mame/mame_libretro.so
          EOF

          chmod +x build_mame.sh
          docker run --rm \
            -v ${{ github.workspace }}:/build \
            -v ${{ github.workspace }}/.ccache:/root/.ccache \
            -e DEBIAN_FRONTEND=noninteractive \
            ubuntu:21.04 /build/build_mame.sh

      - name: Upload Artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: mame_libretro_arm64_smartpro
          path: mame/mame_libretro.so
